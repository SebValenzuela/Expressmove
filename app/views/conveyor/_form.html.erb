<%= form_with(model: conveyor, local: true) do |form| %>
  <% if conveyor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conveyor.errors.count, "error") %> prohibited this conveyor from being saved:</h2>

      <ul>
      <% conveyor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product %>
    <%= form.text_field :product %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.date_select :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
